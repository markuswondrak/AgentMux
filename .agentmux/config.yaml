version: 2

defaults:
  session_name: agentmux
  provider: qwen
  max_review_iterations: 3

roles:
  architect:
    provider: copilot
  web-researcher:
    provider: opencode
  code-researcher:
    provider: opencode
