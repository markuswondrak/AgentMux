version: 2

defaults:
  session_name: agentmux
  provider: copilot
  max_review_iterations: 3

roles:
  coder:
    model: gpt-5.4-high
  web-researcher:
    model: claude-haiku
  code-researcher:
    model: claude-haiku
