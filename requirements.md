we need a ui designer agent. It can be requested by the architect in case there are new us views or components. It should load the frontend-design skill first. model should be claude sonnet. The agent should create a detailed design document. It can create design artefacts like css, html or js but no business logic. it still needs to implement a handover / design document. 

these design documents should be handed to the coder that is responsible for this task that requires it
