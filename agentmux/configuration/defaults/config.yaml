version: 2

defaults:
  session_name: multi-agent-mvp
  provider: claude
  model: sonnet
  max_review_iterations: 3
  completion:
    skip_final_approval: false
  compression:
    enabled: false

github:
  base_branch: main
  draft: true
  branch_prefix: "feature/"

providers:
  claude:
    command: claude
    model_flag: --model
    trust_snippet: Do you trust the contents of this directory?
    role_args:
      architect:
        - --permission-mode
        - acceptEdits
        - --allowedTools
        - Bash(ls:*) Bash(cat:*) Bash(find:*) Bash(grep:*) Bash(git:*) Bash(head:*) Bash(tail:*) Bash(pwd:*) Bash(wc:*) mcp__agentmux-research__*
      product-manager:
        - --permission-mode
        - acceptEdits
        - --allowedTools
        - Bash(ls:*) Bash(cat:*) Bash(find:*) Bash(grep:*) Bash(git:*) Bash(head:*) Bash(tail:*) Bash(pwd:*) Bash(wc:*) mcp__agentmux-research__*
      reviewer:
        - --permission-mode
        - acceptEdits
        - --allowedTools
        - Bash(ls:*) Bash(cat:*) Bash(find:*) Bash(grep:*) Bash(git:*) Bash(head:*) Bash(tail:*) Bash(pwd:*) Bash(wc:*)
      coder:
        - --permission-mode
        - acceptEdits
      designer:
        - --permission-mode
        - acceptEdits
      code-researcher:
        - --permission-mode
        - acceptEdits
        - --allowedTools
        - Read Glob Grep Write Edit Bash(ls:*) Bash(cat:*) Bash(find:*) Bash(grep:*) Bash(head:*) Bash(tail:*) Bash(pwd:*) Bash(wc:*)
      web-researcher:
        - --permission-mode
        - acceptEdits
        - --allowedTools
        - WebFetch WebSearch Read Glob Grep Write Edit Bash(ls:*) Bash(cat:*) Bash(head:*) Bash(tail:*)

  codex:
    command: codex
    model_flag: --model
    trust_snippet:
    role_args:
      architect: [-s, workspace-write, -a, never]
      product-manager: [-s, workspace-write, -a, never]
      reviewer: [-s, workspace-write, -a, never]
      coder: [-s, workspace-write, -a, never]
      designer: [-s, workspace-write, -a, never]
      code-researcher: [-s, workspace-write, -a, never]
      web-researcher: [-s, workspace-write, -a, never]

  gemini:
    command: gemini
    model_flag: --model
    trust_snippet: Trust this folder?
    role_args:
      architect: [--approval-mode, yolo]
      product-manager: [--approval-mode, yolo]
      reviewer: [--approval-mode, yolo]
      coder: [--approval-mode, yolo]
      designer: [--approval-mode, yolo]
      code-researcher: [--approval-mode, yolo]
      web-researcher: [--approval-mode, yolo]

  opencode:
    command: opencode
    model_flag: --model
    trust_snippet:
    batch_subcommand: run
    role_args:
      architect: [--agent, agentmux-architect]
      product-manager: [--agent, agentmux-product-manager]
      reviewer: [--agent, agentmux-reviewer]
      coder: [--agent, agentmux-coder]
      designer: [--agent, agentmux-designer]
      code-researcher: [--agent, agentmux-code-researcher]
      web-researcher: [--agent, agentmux-web-researcher]

roles:
  coder:
    provider: codex
    model: gpt-5.3-codex
